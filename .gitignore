# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Python test cache
__pycache__/
*.pyc

# Local dev servers / logs
*.log

# Working data folder, except the tracked CV source of truth.
data/*
!data/cv.json

# Node tooling. package.json/lock are tracked (CV build + MCP server depend on them);
# node_modules and one-off screenshot scripts stay local.
node_modules/
verify_screenshot.js
*.png

# Throwaway theme-exploration scaffolding (not part of the published site)
examples/
